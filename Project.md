# Project
# KEEP:ON — 프로젝트 컨텍스트

## 0. 이 문서에 대해
당신은 이 프로젝트의 데이터 파이프라인 파트를 함께 개발합니다.
아래 내용을 전부 읽고, 마지막 "첫 작업 지시"를 수행하세요.

---

## 1. 서비스

### 한 줄 정의
저장은 했는데 실행은 안 하는 청년을 위해,
저장한 순간부터 마감까지 끌고 가는 AI 에이전트.

### 문제
만 21~27세 청년은 공모전·지원사업·서포터즈 정보를
인스타그램, 블로그, 학교 공지, 카톡 등에서 각각 발견하고 북마크한다.
그런데:
- 정보가 흩어져 있어 다시 찾지 못함
- 저장은 했는데 다시 열어보지 않음
- 자격이 되는지 확인하려면 공고문을 3장씩 읽어야 함
- 서류 준비와 마감 관리가 귀찮아서 결국 마감을 넘김

핵심은 "찾는 게 어렵다"가 아니라 "찾은 다음이 어렵다"이다.

### 우리가 만드는 것
"추천 서비스"가 아니다. "판정하고 실행시키는 에이전트"다.
- 공고를 대량 수집하는 크롤러를 만들지 않는다
- 사용자가 이미 저장해둔 것을 실행 가능하게 만든다

### 사용자 흐름
1. 온보딩 (30초): 출생연도 · 지역 · 상태 · 소득구간(선택) · 관심사 한 줄
2. 저장: 링크 / 스크린샷 / 텍스트를 넣는다
3. 분석: 에이전트가 내용을 추출하고 자격 조건을 구조화한다
4. 판정: 자격 충족 여부를 근거와 함께 보여준다
5. **사용자 확인** (자동 등록하지 않음)
6. 계획: Quest/Todo + 마감 역산 + 캘린더 등록
7. 실행: 대화로 일정을 조정하고 마감까지 추적

### 타겟 페르소나
김서연 · 24세 · 서울 관악구 · 대학 4학년(컴공) · 미취업 · 세대원
분기당 2~3건 지원. 작년에 공모전 3 · 서포터즈 2 · 부트캠프 1 · 지원사업 1.
등본을 3개월에 4번 뗐고, 관심 있던 서포터즈를 마감 다음날 알았다.

---

## 2. 팀 구조 (5인, 2일 대회)

| 담당 | 역할 | 산출물 |
|---|---|---|
| A 강옥일 | AgentCore 통합 | Supervisor, Profile/Interest Agent, **models.py 소유** |
| **B 신민서 (나)** | **데이터 · 링크 분석** | **공고 데이터, Extraction, Normalization** |
| C 엄세연 | 자격 · 추천 판단 | Eligibility, Feasibility, Ranking |
| D 김나연 | 실행 에이전트 | Planning, Execution Chat, Quest/Todo, 캘린더 |
| E 지수정 | Frontend | Streamlit UI |

### 내 파트의 위치
나는 이 시스템의 **유일한 외부 세계 입구**다.
내 출력이 C(판정)와 D(계획)의 입력이 된다.
스키마가 흔들리면 팀 전체가 멈추므로, 스키마를 임의로 바꾸지 않는다.

### 내 산출물 3개
1. `data/opportunities.json` — 실제 공고 20건 (자격요건 원문 포함)
2. `src/extraction_agent.py` — 링크/이미지/텍스트 → SavedContext
3. `src/normalization_agent.py` — 공고 원문 → 자격 조건 8종 구조화

---

## 3. 기술 환경

- Python 3.11+
- **로컬 실행이 기본.** 클라우드 배포는 대회 마지막 날에만 시도
- LLM: AWS Bedrock (Claude) + Strands Agents SDK
  - ⚠️ 지금은 AWS 계정이 없다. 대회 당일 발급 예정
  - **따라서 LLM 호출 부분은 인터페이스만 만들고, 지금은 목(mock)으로 동작하게 한다**
- 데이터: 로컬 JSON 파일 (DB 없음)
- 검증: pydantic v2
- HTTP: requests + beautifulsoup4
- UI: Streamlit (E 담당)

---

## 4. 자격 조건 8종

| 코드 | 조건 | 공고문에 이렇게 나타남 |
|---|---|---|
| `age` | 나이 | "만 19~34세", "1991년생 이후" |
| `region` | 지역 | "서울시 거주", "주민등록상 6개월 이상" |
| `status` | 학적·취업 | "재학생", "미취업 청년", "졸업 후 2년 이내" |
| `income` | 소득 | "중위소득 150% 이하" |
| `duplicate` | 중복수혜 | "유사사업 기수혜자 제외" |
| `military` | 병역 | "군필 또는 면제" |
| `period` | 신청기간 | "2026.8.1 ~ 8.22" |
| `etc` | 기타 | "세대주", "무주택", "성실히 수행 가능한 자" |

---

## 5. 절대 규칙 (harness)

### R1. 나는 "구조화"만 한다. "판정"하지 않는다.
자격 판정은 C가 코드로 구현한다.
나는 조건을 추출·분류만 하고 적격 여부를 결정하지 않는다.

### R2. 모든 추출에는 근거가 있어야 한다 ★ 가장 중요
조건 하나마다 원문 인용(`raw_quote`)과 원문 내 위치(`span`)를 함께 반환한다.
**원문에 없는 내용을 절대 생성하지 않는다. 추론해서 채우지 않는다.**
모르면 None을 반환한다.

### R3. 실패는 실패로 표현한다.
status는 `ok` / `partial` / `failed` 3값이다.
실패했을 때 그럴듯한 값으로 채우지 않는다.

### R4. 모호한 조건은 해석하지 않는다.
"성실히 수행 가능한 자" 같은 조건은
`type="etc"`, `operator="unknown"`으로 두고 임의 해석하지 않는다.

### R5. 계산에 LLM을 쓰지 않는다.
날짜 차이, 나이 계산 등은 순수 함수로 작성한다.

### R6. 우회하지 않는다.
로그인이 필요한 사이트를 세션 위조·헤더 조작·비공개 엔드포인트로
우회하지 않는다. 실패로 처리하고 스크린샷 업로드를 유도하는 것이
올바른 동작이다.

### R7. 개인정보를 수집하지 않는다.
주민번호, 생년월일, 상세 주소는 받지 않는다.
출생연도, 시군구, 소득 구간으로 충분하다.

## 7. 링크 추출 4단 fallback

사용자가 북마크하는 곳은 인스타그램, 네이버 블로그, 티스토리,
학교 공지사항 등 다양하다. 어떤 링크를 넣어도 결과가 나와야 한다.
**"실패 화면"을 만들지 않는다.**

어느 단계에서 성공했는지 `extract_method`에 기록한다.

---

## 8. 작업 방식

- 코드를 작성하기 전에 **구현 계획을 3~5줄로 먼저 요약**할 것
- **한 번에 한 파일씩.** 여러 파일을 동시에 만들지 말 것
- 스키마를 바꿔야 할 이유가 생기면 **먼저 알려줄 것** (임의 변경 금지)
- AWS 자격증명을 요구하는 코드를 지금 실행하지 말 것
- 파일을 수정하기 전에 무엇을 바꿀지 먼저 보여줄 것

---

## 9. 첫 작업 지시

아래를 순서대로 수행하세요. **각 단계가 끝나면 멈추고 확인을 받으세요.**

### 단계 1 — 프로젝트 폴더 세팅
아래 구조로 폴더와 빈 파일을 만들어주세요.