# Project — KEEP:ON

## 1. 서비스

### 한 줄 정의
저장은 했는데 실행은 안 하는 청년을 위해,
저장한 순간부터 마감까지 끌고 가는 AI 에이전트.

### 문제
만 21~27세 청년은 공모전·지원사업·서포터즈 정보를
인스타그램, 블로그, 학교 공지, 카톡 등에서 각각 발견하고 북마크한다.
그런데:
- 정보가 흩어져 있어 다시 찾지 못함
- 저장은 했는데 다시 열어보지 않음
- 자격이 되는지 확인하려면 공고문을 3장씩 읽어야 함
- 서류 준비와 마감 관리가 귀찮아서 결국 마감을 넘김

핵심은 "찾는 게 어렵다"가 아니라 "찾은 다음이 어렵다"이다.

### 우리가 만드는 것
"추천 서비스"가 아니다. "판정하고 실행시키는 에이전트"다.
- 공고를 대량 수집하는 크롤러를 만들지 않는다
- 사용자가 이미 저장해둔(또는 지금 보고 있는) 것을 실행 가능하게 만든다

### 사용자 흐름
1. **저장**: Chrome Extension의 Keep 버튼(Instagram/Threads 게시물) 또는 웹에서 링크/파일/텍스트 직접 입력
2. **추출**: 원문(제목·본문·링크·게시일)을 정제된 텍스트로 변환
3. **정규화**: 8종 자격조건을 원문 인용 근거와 함께 구조화, 지원사업인지/마감만 있는 정보인지/일반 정보인지 분류
4. **판정**: 자격 충족 여부를 근거와 함께 보여준다 (opportunity 콘텐츠만)
5. **사용자 확인** (자동 등록하지 않음)
6. **계획**: Task 분해 + 마감 역산 + 웹 캘린더 등록
7. **실행**: 대화로 일정을 조정하고, 정체되면 에이전트가 먼저 알림, 마감까지 추적

자동 수집·대량 크롤링·사용자 승인 없는 캘린더 등록은 하지 않는다.
Instagram 릴스와 동영상은 영상을 분석하지 않고 게시물 캡션·본문과 본문 링크만 수집한다.

### 타겟 페르소나
김서연 · 24세 · 서울 관악구 · 대학 4학년(컴공) · 미취업 · 세대원
분기당 2~3건 지원. 작년에 공모전 3 · 서포터즈 2 · 부트캠프 1 · 지원사업 1.
등본을 3개월에 4번 뗐고, 관심 있던 서포터즈를 마감 다음날 알았다.
(그 외 테스트 페르소나 4종은 `docs/personas.md` 참고)

---

## 2. 팀 구조 (5인)

| 담당 | 역할 | 산출물 | 상태 |
|---|---|---|---|
| A 강옥일 | Chrome Extension, Node HTTP 서버, 입력·추출 계층, Supabase Auth, **models.py 소유** | `services/keep-web/` | **구현 완료** |
| **B 신민서 (나)** | **데이터 · 링크 분석, Node↔Python 브릿지** | `data/opportunities.json`, `extraction_agent.py`, `normalization_agent.py`, `bridge_server.py`, `db.py` | **구현 완료** |
| C 엄세연 | 자격 · 추천 판단 | `eligibility_agent.py`, `feasibility_agent.py`, `ranking_agent.py`, `decision_engine.py` | **구현 완료** |
| D 김나연 | 실행 에이전트 | `src/execution/` (Task 분해, Calendar, Notification, 대화형 조정) | **구현 완료** |
| E 지수정 | Frontend | 미정(Node `web/` 또는 Streamlit) | 미착수 |

### 데이터 흐름과 계약
B의 정규화 출력(8종 자격조건 + `raw_quote`/`span` 근거)이 C(판정)의 유일한 입력이고,
C의 출력이 D(계획)의 입력이 된다. A의 Extension/서버는 원문을 추출해 B에게 넘기는
입구 역할이다. **이 스키마 계약이 흔들리면 C/D의 이미 완성된 코드가 멈추므로, 계약을
바꿀 땐 반드시 팀 전체에 먼저 알린다.** 상세 아키텍처는 `ARCHITECTURE.md` 참고.

---

## 3. 기술 환경

- **Node.js 20** (`services/keep-web/`): Chrome Extension(Manifest V3) + 순수 http 서버,
  Intake API, 플랫폼별(Instagram/Threads) 원문 추출
- **Python 3.11+** (`src/`): 정규화, 판정, 실행 에이전트
- **LLM**: Gemini API(`gemini-flash-latest`) 실연동 — AWS Bedrock+Strands 채택 여부가
  아직 불확실해서 우선 Gemini로 실제 동작하게 만들었다. 인터페이스(Protocol)로 감싸져
  있어 AWS 확정 시 구현체만 교체하면 된다 (`.env`의 `GEMINI_API_KEY` 없으면 자동 mock 폴백)
- **DB**: Supabase(Postgres) — `profiles`/`auth.users`(사용자 인증), `intakes`/
  `opportunities`(A 스키마 + B가 추가한 `content_category`/`conditions` 컬럼). RLS 활성화
- **검증**: pydantic v2 (Python), `shared/contracts.js`(Node)
- **UI**: 미정

---

## 4. 자격 조건 8종

| 코드 | 조건 | 공고문에 이렇게 나타남 |
|---|---|---|
| `age` | 나이 | "만 19~34세", "1991년생 이후" |
| `region` | 지역 | "서울시 거주", "주민등록상 6개월 이상" |
| `status` | 학적·취업 | "재학생", "미취업 청년", "졸업 후 2년 이내" |
| `income` | 소득 | "중위소득 150% 이하" |
| `duplicate` | 중복수혜 | "유사사업 기수혜자 제외" |
| `military` | 병역 | "군필 또는 면제" |
| `period` | 신청기간 | "2026.8.1 ~ 8.22" |
| `etc` | 기타 | "세대주", "무주택", "성실히 수행 가능한 자" |

---

## 5. 절대 규칙

### R1. 구조화와 판정을 분리한다
Extraction·Normalization(B)은 원문을 구조화만 한다. 적격 여부 판정은 C가 담당한다.

### R2. 모든 추출에는 근거가 있어야 한다 ★ 가장 중요
조건 하나마다 원문 인용(`raw_quote`)과 원문 내 위치(`span`)를 함께 반환한다.
**원문에 없는 내용을 절대 생성하지 않는다. 추론해서 채우지 않는다.** 모르면 None을 반환한다.
`normalization_agent.py`의 `_verify_grounding()`이 이를 코드로 강제한다 — LLM(Gemini)이
뭘 반환하든 원문에 실재하지 않는 인용은 결과에서 제거된다.

### R3. 실패는 실패로 표현한다
status는 `ok`/`partial`/`failed` 3값이다. 실패했을 때 그럴듯한 값으로 채우지 않는다.
로그인 안내, 게시물 URL 불일치, 본문 미확인, 지원하지 않는 플랫폼은 성공으로 저장하지 않는다.

### R4. 모호한 조건은 해석하지 않는다
"성실히 수행 가능한 자" 같은 조건은 `type="etc"`, `operator="unknown"`으로 두고 임의 해석하지 않는다.

### R5. 계산에 LLM을 쓰지 않는다
날짜 차이, 나이 계산, 마감 역산, 상태 전이는 순수 함수로 작성한다.

### R6. 우회하지 않는다
로그인이 필요한 사이트를 세션 위조·헤더 조작·비공개 엔드포인트로 우회하지 않는다.
실패로 처리하고 스크린샷 업로드(또는 Extension의 Keep 버튼 — 사용자가 이미 로그인한
브라우저에서 보이는 현재 페이지 증거만 사용)를 유도하는 것이 올바른 동작이다.

### R7. 개인정보를 최소화한다
주민번호, 생년월일, 상세 주소, 쿠키·토큰, DM·댓글을 받지 않는다.
출생연도, 시군구, 소득 구간으로 충분하다. (`birth_date`처럼 생년월일 전체를 요구하는
구현이 발견되면 즉시 출생연도 기반으로 되돌린다 — 정밀한 만 나이가 필요해도 이 규칙이 우선한다)

---

## 6. 협업 규칙

- 새 작업은 `feature/*` 또는 이름을 명시한 브랜치에서 진행하고 PR로 main에 합친다.
- 공용 계약(8종 조건 스키마, HTTP API, Node↔Python 브릿지 포맷)을 바꿀 땐 PR 설명에
  영향 범위를 적고 관련 담당자에게 먼저 알린다.
- 다른 담당자가 이미 구현을 완료한 파일은 합의 없이 구조를 바꾸지 않는다. 특히 B의
  8종 조건 구조(`ConditionType`, `EligibilityCondition`)는 C/D가 이미 그대로 쓰고
  있으므로 임의 변경 금지.
- Node 변경은 `cd services/keep-web && npm test`, Python 변경은 `pytest tests/` +
  `python -m scripts.run_pipeline`을 통과해야 한다.
