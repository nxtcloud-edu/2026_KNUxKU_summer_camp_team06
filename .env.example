# 각자 로컬에 .env로 복사해서 채워넣으세요 (.env는 .gitignore에 포함되어 커밋되지 않음)
GEMINI_API_KEY=

# Supabase Project Settings > API Keys 에서 확인
# secret 키 사용 (서버 전용, RLS 우회) — publishable 키 아님, 절대 프론트에 노출 금지
SUPABASE_URL=https://ateajrbkeloomfflnwyx.supabase.co
SUPABASE_KEY=
